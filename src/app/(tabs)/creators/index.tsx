import { View } from 'react-native';
import { Text } from 'react-native-paper';

const CreatorsPage = () => {
  return(
    <View style={{flex:1, alignItems:'center', justifyContent:'center'}}>
      <Text>Creators coming soon!</Text>
    </View>
  );
}

export default CreatorsPage;